<?php $f = $t->_('validate_character_form') ?>
<?= $f->_start() ?>
<?= $f->_errors() ?>
<div>
    <h3>Elements : </h3>
    <?php foreach ($t->_('elements') as $element): ?>
    <p><?= $f->_input($element['value'])->label($element['text'].' :')
        ->number()->min(0)->max(100)->html() ?></p>
    <?php endforeach ?>
</div>
<p><?= $f->_submit('Valider') ?></p>
<?= $f->_end() ?>